Weather Forecast 

A web application that displays real-time weather data using the OpenWeatherMap API.

Tech Stack

React.js
REST API (OpenWeatherMap)

Getting Started Clone the repository: git clone cd

Install dependencies: npm install

Use the correct Node.js version If the project uses an .nvmrc file: nvm use

*If you don’t have the required Node.js version installed: nvm install

Start the development server: npm start
